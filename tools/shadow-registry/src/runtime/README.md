# Package adaptations

Registry-owned finite runtime/install/launch compatibility for existing packages. `index.ts` is the closed SDK composition entry; no runtime-js/npm/Workbench imports. Data catalog never imports this directory. Installed-tree bytes and existing admission remain authority. ADR-0384.
