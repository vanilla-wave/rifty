/**
 * Packed-host scenario carriers (I7 Acc 9 + I1 packed residual).
 * Honest throws until the packed lane produces from an installed
 * workbench tarball and proves planted orphan retain.
 */

/**
 * @returns {Promise<{
 *   snapshotId: string,
 *   tarBytes: Uint8Array,
 *   entry: '@riftydev/workbench/dep-snapshot',
 * }>}
 */
export async function produceFromInstalledWorkbenchTarball() {
  throw new Error(
    'packed produce from an installed @riftydev/workbench/dep-snapshot tarball is not implemented',
  );
}

/**
 * @returns {Promise<{
 *   downloaded: string,
 *   host: {
 *     scope: '/sandbox/',
 *     previewPrefix: '/sandbox/preview',
 *     snapshotOnly: true,
 *   },
 * }>}
 */
export async function provePackedHostOrphanRetain() {
  throw new Error('packed-host planted orphan Scratch download is not implemented');
}
