/** Shared packed-host composition (I7 Acc 9 + I1 packed residual). */
export const PACKED_HOST_COMPOSITION = Object.freeze({
  scope: '/sandbox/',
  previewPrefix: '/sandbox/preview',
  snapshotOnly: true,
  namespace: 'packed-host',
  ownerStartupTimeoutMs: 120_000,
  projectFileTimeoutMs: 120_000,
  sessionToolsTimeoutMs: 120_000,
});
