{
  "name": "@rifty-examples/standalone",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Small, runnable examples that each use one @riftydev package standalone.",
  "scripts": {
    "vfs": "tsx src/01-vfs.ts",
    "semver": "tsx src/02-semver.ts",
    "registry": "tsx src/03-registry.ts",
    "shell": "tsx src/04-shell.ts",
    "start": "pnpm vfs && pnpm semver && pnpm registry && pnpm shell"
  },
  "dependencies": {
    "@riftydev/npm-client": "workspace:*",
    "@riftydev/shell": "workspace:*",
    "@riftydev/vfs": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
