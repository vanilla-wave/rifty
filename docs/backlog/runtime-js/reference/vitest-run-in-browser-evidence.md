# Evidence — vitest-run-in-browser (refine probes, 2026-09-15)

Method: scratch Playwright specs (not committed) over `tests/e2e/helpers/playground.ts`
(`bootShell`/`bootProjectFiles`, `runTerminalLineSettled`, exit code from the
terminal history UI), Chromium, dev server on `RIFTY_PLAYGROUND_PORT=5391`,
main `51440931a`. Guest identity `vitest/4.1.11 rifty-wasm node-v24.0.0`.
Versions: vitest 4.1.11, vite 8.0.16 (7.3.6 / 8.3.0 where stated), jsdom
30.0.1, undici 8.10.2. Temporary shims used to reach the next wall were
reverted; the tree is clean. Full logs lived in `/tmp/vitest-probe/*.log`
(not committed, wiped after the session); excerpts below are verbatim and the
probe scripts' decisive lines are quoted next to each command.

## I1 — install

devDependencies `{"vitest":"4.1.11","vite":"8.0.16"}` (no overrides):

```
npm: + vitest@4.1.11
npm: + vite@8.3.0
npm: install failed: Not implemented: lightningcss.version (shadow recipe does not admit ^1.33.0)
```

Organic unpinned manifest, devDependencies `{"vitest":"4.1.11"}` only, clean tree
(2026-09-16 re-run, unmodified recipe):

```
npm: + vite@8.3.0
npm: install failed: Not implemented: lightningcss.version (shadow recipe does not admit ^1.33.0)   [exit 1, 4.9s]
```

`"overrides": {"vite": "8.0.16"}` (npm spelling):

```
npm: install failed: Failed to fetch packument 8.0.16: 404
```

`"overrides": {"vite": "vite@8.0.16"}` (rifty spelling), clean tree
(`rm -rf node_modules package-lock.json`), devDependencies vitest only:

```
npm: lightningcss@^1.32.0 → lightningcss-wasm@1.32.0 (substituted from shadow registry, ADR-0051)
npm: installed 48 package(s) in 12.1s
$ grep version node_modules/vite/package.json      → "version": "8.0.16"
$ ls node_modules/vitest/node_modules              → No such file or directory (no nested vite)
$ ls node_modules/@rolldown                        → binding-wasm32-wasi  pluginutils
$ vitest --version                                 → vitest/4.1.11 rifty-wasm node-v24.0.0   [exit 0]
```

npm-authored `package-lock.json` replay (lock generated by npm 11 on the host
from the proven `tests/e2e/fixtures/npm-lock-replay/vite8` lock + vitest):
`installed 49 package(s) in 4.1s`, `vitest --version` OK — second path, no
rifty work.

Contaminated probe (recorded, not evidence): with the lightningcss recipe
locally bumped to 1.33.0 and vitest alone, vite 8.3.0 resolved and install
failed `vite root watcher patch failed: expected exactly one Chokidar
DirEntry.add anchor; found 2`. The same message appeared for 8.0.16 when
installing over the starter's `node_modules/vite@7.3.6` without cleaning; on a
clean tree 8.0.16 installs. So "8.3.0 needs a patch re-cut" is unverified;
the stale-dir behaviour is `npm-client/stale-package-dir-on-version-change`.

## I6 — module loading walls (each after shimming the previous)

```
$ vitest run   → ModuleLoadError: Built-in 'node:path/posix' is not implemented      (@vitest/mocker/dist/node.js)
$ vitest run   → SyntaxError: The requested module 'node:fs' does not provide an export named 'statfsSync' (imported by /node_modules/vitest/dist/chunks/cli-api.CnMVyzaz.js)
$ vitest run   → SyntaxError: The requested module 'node:process' does not provide an export named 'cwd' (imported by /node_modules/tinyexec/dist/main.mjs)
```

Static diff of every named builtin import in the vitest 4.1.11 tree against
`loadBuiltin(...)` keys (conformance realm): only `fs.statfsSync` and
`child_process.spawnSync` missing; default-member misses: `process.memoryUsage`,
`process.emitWarning` (deprecation path only), `module.register*` (native mode
only, guarded), `vm.SourceTextModule/SyntheticModule` (vm pools only).

vitest 3.2.7 on vite 7.3.6 (49 packages, installs without overrides):

```
$ vitest --version → NotImplementedError: Not implemented: module-loader.esm-global-function-assignment (ESM module /node_modules/@vitest/utils/dist/index.js writes the Function binding/global property ...)
```

`@vitest/utils/dist/index.js:630` is `globalThis[SAFE_TIMERS_SYMBOL] = timers`
(no `Function` token in the file); vitest 4 carries the same line in
`@vitest/utils/dist/timers.js` (loaded by the test worker). CJS twin:
`undici/lib/global.js` `Object.defineProperty(globalThis, globalDispatcher /* Symbol.for */, …)`
→ `module-loader.cjs-global-function-assignment` (blocks `import('jsdom')`).

## I2 / I3 — lifecycle

```
$ vitest run                     → (no output)                                   [exit 0, 1941ms]
$ DEBUG=vitest:* vitest run      → (no output)                                   [exit 0]
$ node node_modules/vitest/dist/cli.js run --reporter=verbose → (no output)      [exit 0, 1957ms]
```

Instrumented (`t14.mjs` wraps `process.exit`, stdout/stderr writes, imports
`vitest/dist/cli.js` with argv `run`):

```
exit called code=0 exitCode=0
    at process.exit (/t14.mjs:8:135)
    at Object.exit (.../packages/workbench/src/workers/node-entry-bootstrap.ts:110:26)
    at runNodeProgramLifecycle (.../packages/workbench/src/workers/node-program-lifecycle.ts:91:14)
out.log: "\n[cli-imported]\n"   err.log: (absent)
```

Worker keepalive (`w.cjs`: `setTimeout(() => parentPort.postMessage('hi'), 700)`;
`t3.cjs`: `new Worker('./w.cjs')` + `on('message'|'exit')`, `process.on('exit')`):

```
$ node t3.cjs → (no output)                                                     [exit 0, 427ms]
```

Kept-alive controls: non-awaited `import('vite')`, `setTimeout` → `import()`,
`WebAssembly.instantiate(...)`, `MessageChannel` message, `fs.readdir`
callback/promise all complete and print (`k1.mjs`…`k4.mjs`, `t8`–`t11.mjs`).

Programmatic start from a top-level-await program (`t2.mjs`:
`const v = await m.startVitest('test', [], { run: true })`):

```
start
vitest-node-imported function

 RUN  v4.1.11 /

Uncaught TypeError: dest.end is not a function (.../packages/io/src/streams/readable.ts:545)   [exit 1, 1936ms]
```

Raw fork of the worker entry
(`fork('node_modules/vitest/dist/workers/forks.js', [], {serialization:'advanced', stdio:'pipe'})`,
with the advanced throw bypassed):

```
child-err TypeError: Cannot read properties of undefined (reading 'bind')
    at eval (/node_modules/vitest/dist/chunks/init.k9zZ9sLh.js:202:41)      ← process.memoryUsage.bind(process)
child-exit 1 null
```

Process events / exit code:

```
$ node -e "process.exitCode=3"                                   → [exit 3]
$ node -e "process.exitCode=3;process.exit()"                    → [exit 0]      (Node: 3)
$ node t5.mjs  (on('uncaughtException', log); throw in timer)    → Uncaught Error: boom (/t5.mjs:6)  [exit 1]; handler not called, 'after' not printed
$ node t7.mjs  (on('unhandledRejection', log); Promise.reject)   → Error: rej                          [exit 1]; handler not called
$ node t0.mjs  (process.on('exit', log))                         → pathe-imported                      [exit 0]; no EXIT line
$ node t1.mjs  (beforeExit/exit handlers; vite createServer)     → start / vite-imported / server-created / closed; no BEFORE-EXIT, no EXIT
$ node -e "const r=require('stream').Readable.from(['a']);r.pipe(process.stdout)" → aTypeError: dest.end is not a function [exit 1]
$ node -e "console.log(typeof process.memoryUsage, typeof process.emitWarning, typeof process.setSourceMapsEnabled, typeof process.stdout.end)" → undefined undefined undefined undefined
```

## I4-amended — the claimed scenario on main (2026-09-16)

Clean tree; manifest devDependencies `{"vitest":"4.1.11"}` + `"overrides":
{"vite":"vite@8.0.16"}` (rifty spelling — npm spelling fails on main, §I1);
`vitest.config.ts` = `import { defineConfig } from 'vitest/config'; export
default defineConfig({ test: { include: ['src/**/*.test.ts'] } })`;
`src/sum.ts`; `src/sum.test.ts` (one passing, one failing test):

```
$ npm install   → installed 48 package(s)                                                        [exit 0]
$ vitest run    → Startup Error / ModuleLoadError: Built-in 'node:path/posix' is not implemented  [exit 0]
$ npm test      → > vitest run / same Startup Error                                              [exit 0]
```

The first wall precedes config loading and file collection (I6 → I4); exit 0
after a startup error is the I3 `process.exit()`/`exitCode` defect.

## I4 / I5 — pools (static + shimmed runs)

- vitest 4.1.11 `cli-api` fork: `fork(entry, [], { env, execArgv, stdio: "pipe", serialization: "advanced" })`;
  `child_process.ts:324` → `NotImplementedError('child_process.serialization.advanced')`.
- threads: `new Worker(entry, { env, execArgv, stdout: true, stderr: true })` then
  `thread.stdout.pipe(...)`; `worker_threads.ts:111` throws
  `worker_threads.Worker.execArgv` for any own `execArgv` (even `[]`);
  `new Worker` from `node -e` throws the same (eval launch inherits execArgv).
- module evaluator (vitest 4 `module-evaluator.js`, vite-node 3 `client.mjs`):
  `vm.runInThisContext(wrapped, { filename, lineOffset: 0, columnOffset: -N })`
  → `vm/index.ts:199` `NotImplementedError('vm.runInThisContext.columnOffset')`.
- `vitest run --pool=threads|vmThreads|vmForks` all stop at the same
  main-process walls as forks (I6, then I2).

## jsdom (deferred to `epics/jsdom-environment-in-browser`)

devDependencies `{"vitest":"4.1.11","jsdom":"30.0.1"}` + overrides vite@8.0.16:
`installed 85 package(s) in 15.1s` (canvas optional peer skipped). With both
Function guards temporarily bypassed:

```
$ node j3.mjs  new JSDOM('<p>hi</p>')                                  → no-scripts p: hi          [exit 0]
$ node j1.mjs  new JSDOM(..., { runScripts: 'dangerously', ... })      → TypeError: Cannot read properties of undefined (reading 'DONT_CONTEXTIFY')   [exit 1]
$ node j2.mjs  builtinEnvironments.jsdom.setup(globalThis, {})         → same TypeError
```

jsdom 30 `lib/jsdom/browser/Window.js:58`: `window = vm.createContext(vm.constants.DONT_CONTEXTIFY)`;
vitest's jsdom environment defaults `runScripts = "dangerously"` (destructuring
default — `undefined` cannot opt out).

## Oracle — real Node v24.16.0 (host, 2026-09-16)

Oracle runs per claim. `t3.cjs`, `t5.mjs`, `t7.mjs` mirror the rifty scripts of
the same name above; `t0.mjs` adds a `beforeExit` handler to rifty's `t0.mjs`
(rifty's exit-event probes were `t0.mjs`/`t1.mjs`). `pipe.cjs`, `fork.cjs`,
`wstd.cjs`, `vmoff.cjs` are Node-only oracle scripts whose rifty counterparts
were the inline `node -e` pipe one-liner, the raw fork of vitest's own
`workers/forks.js`, and static source reads of the option shapes (§I4 / I5).

```
$ node -e "process.exitCode=3;process.exit()"; echo $?                       → 3
$ node t3.cjs   (Worker posts 'hi' after 700 ms; parent logs message/exit)   → got hi true / wexit 0 / EXIT 0      [exit 0]
$ node t5.mjs   (on('uncaughtException'); throw in timer)                     → caught boom / after                 [exit 0]
$ node t7.mjs   (on('unhandledRejection'); Promise.reject)                   → caughtR rej / after                 [exit 0]
$ node t0.mjs   (beforeExit + exit handlers)                                  → a / BEFORE-EXIT / EXIT 0            [exit 0]
$ node pipe.cjs (Readable.from(['a\n']).pipe(process.stdout); later write)   → a / still-writable / end-fn function [exit 0]
$ node fork.cjs (fork(child, [], {serialization:'advanced', stdio:'pipe'}); send Date/Map/[undefined]/Uint8Array; send(fn))
                                                                             → fn-send ERR_INVALID_ARG_TYPE / roundtrip [true,true,true,true] true true
$ node wstd.cjs (new Worker(src, {eval:true, execArgv:[], stdout:true, stderr:true}))
                                                                             → types object function object / captured "from-worker\n"
$ node vmoff.cjs (runInThisContext with {lineOffset:0, columnOffset:-20} / {lineOffset:10, columnOffset:5})
                                                                             → at /virtual/mod.js:1:1 / at /virtual/mod2.js:12:21
$ node --input-type=module -e "import { join } from 'node:path/posix'; import { cwd } from 'node:process'; import { statfsSync } from 'node:fs'; import { spawnSync } from 'node:child_process'; console.log(join('a','b'), typeof cwd(), typeof statfsSync, typeof spawnSync, typeof process.memoryUsage)"
                                                                             → a/b string function function function
$ npm --version                                                              → 11.17.0
$ npm install --package-lock-only --ignore-scripts   (package.json: devDependencies {vitest 4.1.11}, overrides {"vite": "8.0.16"}, registry.npmjs.org)
$ node -e 'const l=require("./package-lock.json");const v=Object.entries(l.packages).filter(([k])=>k.endsWith("node_modules/vite"));console.log("vite entries:",v.map(([k,e])=>k+"@"+e.version).join(", "));console.log("vitest dep on vite:",l.packages["node_modules/vitest"].dependencies.vite)'
                                                                             → vite entries: node_modules/vite@8.0.16 ; vitest dep on vite: ^6.0.0 || ^7.0.0 || ^8.0.0
  (npm 11 resolves the bare-version override to one hoisted vite 8.0.16 for vitest's `^8` edge)
```

vitest 4.1.11 static cites for the lifecycle needs: worker `chunks/init.k9zZ9sLh.js:115`
`processOn("uncaughtException", …)`; `chunks/cli-api.CnMVyzaz.js:2081`
`process.on("unhandledRejection", …)`, `:2063` `process.once("exit", onExit)`,
`:2059`/`:2079` and `chunks/cac.uFydS1Z4.js:2348` `process.exitCode = …; process.exit()`.
No `beforeExit` listener in the tree.

Note on line numbers: stack frames quoted from rifty runs (e.g. `readable.ts:545`,
`node-program-lifecycle.ts:91`) are Vite dev-server bundle offsets; the goal cites
the current source lines (`readable.ts:768`, `node-program-lifecycle.ts:155`).
