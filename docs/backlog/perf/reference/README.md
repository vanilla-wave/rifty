# perf reference docs

The `ADR-00NN` numbers in these perf plan docs (and the perf backlog item slugs) are PROVISIONAL plan labels — they are NOT reserved as 0081-0093 on disk. Each gets a real, then-current ADR number when authored via `pnpm adr:new perf "..."`.
