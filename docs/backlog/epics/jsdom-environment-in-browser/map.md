# Map — jsdom-environment-in-browser

Draft goal (just-file); no children seeded until its refine settles the
environment question and the vm-realm spike answers.

## Items

(none)

## Open questions

- owner: user — is jsdom the environment that matters, or happy-dom
  (different vm dependence)? Asked at this epic's refine.
- owner: agent — spike: jsdom 30 Window on `vm.createContext(DONT_CONTEXTIFY)`
  over the QuickJS realm; record survive/fail per interface family.

## Out of scope

- decided at refine
