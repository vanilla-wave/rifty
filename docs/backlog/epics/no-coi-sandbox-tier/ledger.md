# Ledger — no-coi-sandbox-tier (append-only)

- 2026-08-28 — fitted from refine-closed draft `distribution/no-coi-sandbox-tier` (interview 2026-08-25..28, decisions carried to goal.md; draft deleted this commit — evidence lives in goal + `runtime-js/reference/no-coi-degradation-probes.md` + spike branches).
- 2026-08-29 — runtime-js/worker-realm-compat-bare-sab-referenceerror band 3–5 expected REDs (first no-COI browser substrate: decode bytes / no-arg decode / install no-op, + green pins; COI unit tests stay green).
- 2026-08-29 — bare-sab-guard Contract+RED checkpoint 1: 14-blocker batch → re-cut in place (same branch, attempt 2). Direction flip: no-op guard killed — real probe (Chromium 148.0.7778.96, headerless page + Worker) shows shared `WebAssembly.Memory` constructs no-COI and native decode rejects its views where Node v24.16.0 decodes; fix = unconditional realm-safe patch (ADR-0162 intact). Band re-declared 5–8 expected REDs (was 3–5; probe-widened input classes: shared-wasm view/raw buffer/streaming + Worker aggregate).
- 2026-08-29 — sweep corrections carried by the re-cut: util-types map question settled (brand-based, Node-identical no-COI — map edited); spawnSync/execFileSync ABSENT exports (only execSync has the named loud throw) → routed to `runtime-js/node-builtins-loud-stub-capability-gaps`; public `createSabRing`/`spawnKernelWorker` raw no-COI `ReferenceError` → named-gate/report scope recorded on build-loop. Evidence: `runtime-js/reference/no-coi-degradation-probes.md` §2026-08-29.
- 2026-08-29 — bare-sab-guard Contract+RED checkpoint 2: 5 blockers (C1 missing committed substrate RED, C2 missing exactness pins, C3 boolean-only repeat pins, C4 non-replayable probe provenance, G1 uncertified I2 mapping) → fault-classes §Contract escalation (2nd consecutive) → batch re-cut in place, attempt 3, re-refine WITHOUT split. Substrate committed RED: `playwright.no-coi.config.ts` + `tests/no-coi/` — 7 RED (parity 1–7, all `ReferenceError: SharedArrayBuffer is not defined`) + 2 green pins (preconditions, parity 10), inside the declared 5–8 band. COI pins added green (`worker-realm-compat.test.ts`): parity 8 sentinel/offset (Uint8Array, DataView, raw SAB), parity 9 exact input/opts/error identity, parity 7 repeat-install identity (direct + aggregate). Probe replayable: `tools/probes/no-coi-realm-probe.mjs` + transcript `reference/no-coi-realm-probe-transcript-2026-08-29.json` (Chromium 148.0.7778.96 + node v24.16.0 rows regenerated, parity 7–9 evidence added). G1: I2-prerequisite mapping corrected on map (public worker entry installs no realm-compat; organic certification recorded on build-loop).
- 2026-08-29 — bare-sab-guard Contract+RED checkpoint 3: 12 blockers → batch re-cut in place, attempt 4, no split. Pins strengthened (never weakened, same oracles, still 7 RED + 2 green in band 5–8): raw-buffer row exact length+SHA-256 (`c0a9261d…`, lossy projection dropped); aggregate sibling effects snapshot at call ONE incl. `self` pre-write value + `Object.hasOwn` + writable-data descriptor (probe, spec parity 6, COI unit); `patched.sharedDataView` asserted in all 4 combos + NATIVE DataView differential row (Chromium rejects / Node `'hello'`); parity-10 Node column now REAL `node:util/types` differential (`utilTypesNative` transcript rows, binding intact+deleted). Transcript regenerated same command. Harness headers trimmed; unused server root knob cut. Goal drift fixed: I2 organic-reachability = map Open question "installNodeRuntime seam" (was a deferred certification claim); build-loop + dev-hmr UNSEEDED per FIT §5 (contracts depended on open fog; drafts deleted — scope on map Unseeded lines, durable evidence: probes reference doc + `distribution/reference/no-coi-hmr-spike-record.md` + ADR-0316); map item-1 narrative + settled util-types paragraph cut (index, not store).
- 2026-08-29 — bare-sab-guard Contract+RED checkpoint 4: 10 blockers + 2 missing coverage rows → batch re-cut in place, attempt 5, no split. Pins strengthened (never weakened, same oracles — now 8 RED + 2 green, top of band 5–8): parity 12 poisoned-binding sweep (`SharedArrayBuffer` redefined as counting+throwing accessor AFTER install; count must be EXACTLY 0, today 6 in all 4 realm×install combos + Node sims — kills the try/catch-over-bare-identifier pass of output-only rows; probe row 13; B3). B4: pre-existing installWritableSelf test restored byte-identical to main (branch diff of `worker-realm-compat.test.ts` is additions-only; strengthened pin split into a SEPARATE test) — parity 11 / acceptance regain their unmodified-baseline carrier. B5: probe row 12 re-cut to bundle the PUBLIC `kernel/src/index.ts` entry — sweeps `createSabRing` + `spawnKernelWorker` (dies at its first constructor, spawn-worker.ts:395, pre-Worker) + retained `createWorkerOutputState` (worker-stdio-drain.ts:119); transcript regenerated, replay byte-identical modulo timestamp. Authority gaps closed with ADR carriers: ADR-0367 (semantic capability report ADDITIVE — public `checkCapabilities` platform probe untouched; restart primitive + worker-died event beside the dispose()-only lifecycle; cpus→1 divergence; degradation shape), ADR-0368 (OPFS backend selection drops the ADR-0072 `crossOriginIsolated &&` clause — dated correction note + README Corrections row, not a postponed draft), ADR-0369 (dedicated headerless no-COI Playwright lane); goal/map now cite the ADRs instead of goal fiat. Map re-charted: leftover build-loop/dev-hmr pseudo-items dissolved into UNSLICED fog (FIT §5 — they carried deliverables/ordering atop open fog); installNodeRuntime-seam settlement re-assigned from the owning slice's own later Contract+RED (illegal fog→pickup transition, B6) to a DELIVERABLE of the first slice composing the no-COI runtime.
- 2026-08-30 — bare-sab-guard Contract+RED checkpoint 5: 9 blockers → batch re-cut in place, attempt 6, no split. Wrapper-shaped holes closed (pins strengthened, never weakened, same oracles — now 11 RED + 2 green, band re-declared 8–12): parity 13 ordered exact-call log (COI unit, direct+aggregate — original decoder invoked EXACTLY once per decode; the only shared-source call receives a private copy with exact bytes/opts; unique sentinel returns — kills try-native/catch/copy-retry, which output+error-identity rows alone admitted); parity 9 unique-sentinel RETURN pass-through (COI + no-COI) + shared-wasm FIRST-error identity with throw count 1 (probe `identity.errorIdentitySharedFirst` — the frozen transcript's only errorIdentity row was non-shared, `provenance-lie`); parity 14 mixed direct-then-FIRST-aggregate sequence page+worker (marker-keyed aggregate early return would skip global/self — today siblings green, decode RED); parity 15 realm-decoder non-shared DataView/ArrayBuffer rows both install modes; probe row 12 kernel sweep re-armed with a counting `Worker` constructor — `workerConstructions: 0` per public entry, replay fails loud on nonzero; transcript regenerated same command (Chromium 148.0.7778.96 / node v24.16.0). Process/authority: ADR-0369 lane wired as REQUIRED CI job `no-coi-chromium` + CI gate (opt-in lanes never close acceptance; RED on the draft until the fix) — ADR-0369 consequence corrected; `workers:1`/`fullyParallel:false` knobs cut (§Simplicity, no forcing constraint); ADR-0367 §4 now RECORDS superseding ADR-0011's same-realm-fallback "non-isolated test environments only" clause (dated correction note on ADR-0011 + README Corrections row); goal.md restored byte-identical to main — post-pickup goal edits reverted (goal is user-owned; ADR citations live on map/ADRs/ledger).
- 2026-08-30 — bare-sab-guard Contract+RED checkpoint 6: 4 blockers → batch re-cut in place, attempt 7, no split. Pins strengthened (never weakened, same oracles — now 12 RED + 2 green, top of band 8–12): substrate response-header provenance — probe body fetches EVERY consumed response class (document/Worker script/probe module/built bundles) and fails loud unless BOTH COOP and COEP are absent (derived `crossOriginIsolated===false` alone passed a one-header server, `provenance-lie`; probe row 16, green preconditions pin); shared first-error coverage widened from one generic-Error Uint8Array row to a fresh-TypeError sweep across shared view/DataView/raw/streaming, page+worker × direct/aggregate + COI vitest twins (catch-TypeError and class-scoped native-first retries killed; probe row 18); parity 13 ordered exact-call log extended to the FULL class set (no-arg, priv DataView/ArrayBuffer, streaming pair with exact per-call opts) in vitest direct+aggregate AND given a substrate carrier `exactCallLog` through both REAL install paths — RED today (probe row 17; SAB-present-only streaming/DataView/raw native-first branches killed). Transcript regenerated same command; `worker-realm-compat.test.ts` still additions-only vs main (16 green). Goal drift reconciled: unpicked draft `vfs/no-coi-opfs-policy-flip` re-cut to ADR-0368's recorded selection predicate (`OpfsVfs.isSupported()` platform probe; its OpfsFsSync-as-predicate requirement + stale pickup-ADR scope dropped; sync capability keeps the loud `OpfsFsSync.init` NotImplementedError guard; e2e reload proof re-routed from the dissolved build-loop item to composition fog).
- 2026-08-30 — bare-sab-guard Contract+RED checkpoint 7: 7 blockers (5 completeness, 1 approach-cost, 1 budget) → batch re-cut in place, attempt 8, WITH SPLIT: lane mechanics / header provenance / required CI job / evidence driver moved verbatim-or-stronger to new ready `toolchain-build/no-coi-substrate-lane` (map item 2; green, tooling-class; split re-cut names its predecessor — nothing weakened, no demotion). Pins: fresh-error first-object + count-1 sweeps extended to private classes/no-arg AND the realm global TextDecoder via both real carriers (transcript `identity.errorFirstPrivate` + `errorFirstRealm`, COI vitest twins — 18 green); header provenance re-based from the in-page fetch sweep (blind to a `Sec-Fetch-Dest`-keyed server) to ACTUAL consumed-response observation with per-class × per-header destination-conditional injection controls (`tests/no-coi/header-provenance.mjs` + 10 green control tests); CI job→script→config→gate mapping pinned with a sibling sweep incl. `continue-on-error` absence (`ci-change-scope.test.ts`); kernel public-entry sweep given GOLDEN assertions (function export + exact `ReferenceError: SharedArrayBuffer is not defined` + zero Worker constructions — replay fails loud on removed exports/success/wrong errors). Budget: pickup band 3–5 had grown to 12 REDs with only band re-declarations — README §Goal run ("expected-RED batch far above the declared band → the unit is too big: re-cut/split before implementation") now executed as the split; bare-sab-guard band re-declared as EXACTLY its 12 committed substrate REDs (growth was review-added wrapper-killer pins on the one fix carrier, not scope). Approach-cost: ledger rows 7–11 + unit Decisions compacted in place to lineage one-liners (no checkpoint dropped, full text in git history; one authority = unit contract + transcript). Transcript regenerated same command (Chromium 148.0.7778.96 / node v24.16.0); lane still 12 RED + 2 green + 10 injection pins.
- 2026-08-30 — ledger repair (checkpoint 8): checkpoint-7's in-place compaction of rows 7–11 violated append-only ("`ledger.md` only grows" — backlog README §Goal run); the five original checkpoint rows are restored verbatim above and row 12's "compacted in place" clause is void — no ledger row is ever rewritten again. Unit-doc Decisions stay compacted (a contract is re-cut in place; it is not append-only).
- 2026-08-30 — toolchain-build/no-coi-substrate-lane band 0 expected REDs (tooling-class: green pins with separately pinned DETECTION — header-injection, absent/non-200, replay goldens; fault-classes §Review convergence scopes RED-first to parity/stateful work). Declared retroactively: the checkpoint-7 split carried the item without its own band row (rifty-review axis 5).
- 2026-08-30 — bare-sab-guard Contract+RED checkpoint 8: 9 blockers (2 bugs, 3 completeness, 2 goal-drift, 2 budget) → batch re-cut in place, attempt 9. Carriers strengthened (never weakened): consumed-class detection given absent AND non-200 arms with DISTINCT loud messages, pinned through page/worker/kernel-driver caller sets (6 controls; server status-inject knob); kernel goldens now require an ACTUAL realm ReferenceError — instanceof + prototype + constructor (name/message projection passed a fabricated `Object.assign(new Error(msg), {name})`) — AND one exact TOTAL zero-Worker counter spanning import+setup+all three calls beside the per-entry deltas; parity 9 fresh-error sweeps extended to explicit `{stream:false}` siblings + the streaming FINAL call (first error, count 1, original invoked EXACTLY 2×) across injected/realm × direct/aggregate × substrate/COI-vitest carriers (22 vitest green); runProbe preconditions now GATE (incl. shared-memory brand + instanceof) BEFORE any built-module import/install/decode, with wrong-brand rejection pins + side-effect sentinels (no /dist/ request, decode unmarked) in page AND worker. Split made SERIAL — the map cycle (bare-sab consumed the lane while the lane's required job stayed red until the fix) is killed: the expected-RED batch is runner-DECLARED via `test.fail(true, …)` — every RED still EXECUTES and must fail, an unexpected pass fails the lane LOUD, the fix PR strips exactly the annotations — so `pnpm test:no-coi` is green NOW (32: 12 declared-RED + 20 pins) and the lane item lands before the fix; map reordered lane-first. Budget: the over-band unit (12-block batch vs 3–5 pickup band) has no honest in-place cure left — every candidate split of the RED batch yields a unit with no independent deliverable (one atomic patched-body fix; §Simplicity + "one PR = one reviewable delivered behavior"), band re-declaration was rejected at checkpoint 7's review, trimming substrate pins is observable weakening → bare-sab-guard DEMOTED to draft with the fork recorded and pre-demotion Acceptance/Parity verbatim (`runtime-js/reference/bare-sab-guard-pre-demotion-2026-08-30.md`); manual `rifty-refine` requested (decision-workflow §Backlog readiness 5; fault-classes §Contract escalation, re-refine arm). Transcript regenerated same command (Chromium 148.0.7778.96 / node v24.16.0).
- 2026-08-30 — no-coi-lane Contract+RED checkpoint 9: 9 blockers (2 completeness on provenance, 2 mission/architecture, 2 bugs, 1 completeness on the contract doc, 1 budget, 1 regression) → batch re-cut in place, attempt 10. Lane contract: `## Reference contract` added (Chromium 148.0.7778.96 via Playwright launch + Node v24.16.0 fresh-child oracle, mechanisms pinned). Provenance re-based to (path, `Sec-Fetch-Dest` destination) matching — pathname-only accepted a missing/destination-only-404 real class shadowed by an ordinary 200 fetch; absent/non-200 arms pinned by EXACT mutually exclusive messages (a combined diagnostic passed two lazy regexes); controls re-cut: 3 absent (real destination never consumed + clean same-path fetches, per caller set) + 4 dest-only-404 (document/worker/module kinds; server status-inject now dest-keyed) + exact class→(path,dest) identity/uniqueness pin + injection sweep widened to every unique consumed path incl. kernel bundles (14) — pathname-only mutation fails all 7 new controls, positives stay green (verified run). CI sweep re-based from forbidden substring pins to parsed exact executable values (step-run array membership, whole-script equality, imported-config testDir, exact gate env map, YAML-key continue-on-error absence) — `pnpm test:no-coi || true` and `run: true` now fail. Budget: the draft predecessor's carriers left the branch — 12 declared-RED blocks + parity-10 pin stripped from the lane spec, 663-line COI vitest additions reverted byte-identical to main (a draft is never implemented; verbatim: pre-demotion reference + `67cc346`); the checkpoint-8 `test.fail` greening device WITHDRAWN — ADR-0369 dated correction + README Corrections row record red-until-fix binding READY substrates from their Contract+RED commit. Regression: frozen transcript excluded from Biome formatting (driver-owned byte-identity; precedent npm-11 probe output) — lint green. Verified: `pnpm test:no-coi` 25 green; driver replay byte-identical modulo timestamp; ci-change-scope vitest green.
- 2026-08-30 — no-coi-lane Contract+RED checkpoint 10 (review addressed to the goal branch under the predecessor's unit name bare-sab-guard; every surviving carrier is lane-owned): 13 blockers (5 completeness, 2 mission/architecture, 4 regressions, 1 CI false-fallback, 1 missing CHANGELOG) → batch re-cut in place, attempt 11. Regressions: origin/main merged back into the branch — it had fallen behind the landed installer decomposition (PR #288) + process re-cuts (#287, #276), so raw-comparison reviews read landed main work (installer.ts donors, file-size pin deletion, AGENTS.md refine window, FIT probe-or-fog scope, review scheduling, installer-decomposition closure) as branch deletions; post-merge those files are byte-identical to main. Completeness: fixture provenance made STRUCTURAL (esbuild metafile entryPoint per bundle pinned to literal `packages/` sources + copy-path/missing/unconsumed mutant throws; Node oracle siblings identity-checked vs real `node:util` `.types`, both binding-intact and deleted children, transcript field + goldens); header detection extended with the every-class-position pure sweep (24 failing positions — kills the first-non-document-only validator all 25 prior tests passed) and per-path clean-fetch 200 asserts (tail-404 premise); precondition rejection swept per predicate sibling (coi/sab/brand/instanceof) × page+worker with an ACTUAL decode counter EXACTLY 0 (marker + /dist/-request sentinels were blind to an unpatched native decode before the gate); CI sweep re-based to STEP-scoped parsing + semantic YAML mutant sweep (disabled exact step, quoted `'continue-on-error'`, detached gate env — 11 mutants all rejected) + imported-config topology (globalSetup/webServer command/no narrowing knobs) + `playwright --list` discovered-spec identity + executed globalSetup builder hop. CI false-fallback: `no-coi-chromium` made UNCONDITIONAL (never in ADR-0323 §3's classified set) and gate-required `success` on code/docs-only/classifier-failure paths + event classification sweep (push/merge_group/pull_request) — a docs-only classification can no longer green a READY RED substrate between Contract+RED and fix. Root CHANGELOG entry added (reversible lane+CI delivery). Verified: `pnpm test:no-coi` 37 green; ci-gate + ci-change-scope vitest 18 green; driver replay clean, transcript regenerated same command (Chromium 148.0.7778.96 / node v24.16.0) — field diff vs frozen: the two `nativeUtilTypesProvenance` rows only.
