# Ledger — fast-project-open-reopen (append-only)

- 2026-09-01 — fitted from refine-closed drafts `vfs/segmented-opfs-replica` + `perf/project-vfs-targeted-page-reads` (interview 2026-08-31..09-01; five user forks carried to goal.md §Decisions). Evidence: `vfs/reference/storage-journal-design-benchmarks-2026-08-31.md`, `vfs/reference/storage-open-reopen-candidate-benchmarks-2026-09-01.md` — PR #298 closed unmerged, both reference docs ride this PR.
- 2026-09-01 — observed: per-file handle open is half of reopen (C1 cached burst: open 2,071 ms vs read 1,497 ms, ≈0.14 ms/file) — any per-file layout floors at 2–3 s on S.
- 2026-09-01 — observed: no persisted "dirty" mark affects open/reopen (13 mechanisms swept; only `scratch.dirty` in `catalog.json` and `durability: 'pending'` in the stamp persist, both O(1) reads).
- 2026-09-01 — observed: `git.status()` on open does not hash `node_modules` (vendored isomorphic-git 1.38.5 skips `.gitignore`d subtrees) — suspicion removed, not a lever.
- 2026-09-01 — observed: `apps/playground/src/glue/app-project-store.ts` `createAppProjectStore` (+ `page-store.ts` `markDirty` / `onScratchDirty`) is re-exported from `App.tsx` but never instantiated in-repo — dead-code candidate; rides the first playground-touching slice, not this docs PR.
- 2026-09-01 — challenge: 10 problems, 1 blocking (cheaper direct authority: re-apply a matching baked snapshot on reopen instead of persisting `node_modules` — route R). Goal stays `draft`; fork recorded as OPEN in goal.md §Decisions + map fog → `rifty-refine`. Advisory 2–6 answered in goal.md; 7 → touch-fraction probe; 9 → map re-cut (replica = fresh-format projects; legacy re-materialization + route R unseeded).
- 2026-09-01 — re-verified against the real embedder (tracker4, workbench 0.4.0): S's 98.2 MB was base64 — real snapshot T = 216 pkgs / 15,568 files / 73.6 MB logical / 28.5 MB gz; real cold open 16.3 s (0.3.0: ≈50 s, 40.4 s promotion); reopen never timed; two wasm blobs = 37.5 % of bytes. Benches ≈2× optimistic on the whole open; drain share on 0.4.0 unknown. Substrate flips S → T. Full record: `vfs/reference/tracker-embedder-snapshot-facts-2026-09-01.md`.
- 2026-09-01 — observed: embedder seed needs `/.gitignore` or the git baseline walks `node_modules` and exhausts the owner budget — a hidden open-path precondition no benchmark covers.
- 2026-09-01 — decided (user): route R fork → option 3, replica persists every tree; staged A (base segment + replay, per-file mutations) → B (append + compaction); playground legacy projects lost loudly, no export prompt. Full answer: goal.md §Decisions.
- 2026-09-01 — FIT complete: status ready; map re-cut to 4 slices (targeted-page-reads → replica-base-segment → replica-append, legacy-cold-restore); blocking challenge problem closed by recorded override.
- 2026-09-01 — challenge on seeded children: slice B 2 blocking (value scenario unsized; compaction cheaper as base re-emission from the live front) → answered, goal I5 added; legacy item 1 blocking (namespace bump `/.rifty/workbench/v1→v2` + existing `project-materialization.ts` open path already re-materialize) → taken, item re-scoped to notice/playground honesty; bounded ADR-0165 §10 migration reuse recorded as not taken (user: breaking change).
- 2026-09-01 — measured on main (real T `node_modules`, T′ carriers): cold open 13.37 s = drain 11.47 s (85.8 %) + non-storage 1.10 s; reopen 10.03 s = walk 2.24 + preload 6.17 (83.9 %) + fleet boot ~1.0; benches optimistic 1.49× / 2.03×; storage owns the open. Fog line "split of 16.3 s" resolved. Full record: `vfs/reference/tracker-snapshot-open-split-2026-09-01.md`.
- 2026-09-01 — observed: the 0.4.0 tracker asset is unrestorable on main (install-artifact identity mismatch; shadow catalog drift → `EBROKENLOCK`); acquisition falls to install, registry unreachable, `openProject` returns in 295 ms with no `node_modules` — recorded in `playground/baked-snapshot-regeneration`.
- 2026-09-01 — observed: embedder's `createScratch({preserveDirtySameStarter:true})` on every open resets a CLEAN scratch → erase 17k files + full re-materialization, 40.7 s (worse than cold) — minted `playground/create-scratch-clean-same-starter-rematerializes`.
- 2026-09-02 — user asked whether honest pnpm removes the problem: no — cost is per physical file at OPFS, pnpm dedups across projects/installs (prior record `perf/reference/dependency-store-and-vfs-links.md`); recorded as not-a-lever in goal.md §Decisions.
- 2026-09-12 — re-checked against main (154 commits since 1a851d7bc; user kept the plan, docs actualized): I1 path unchanged (ADR-0358). I2 baseline stale in shape — ADR-0393 (09-08) replaced the two-pass `refreshIndex` + `preloadContent` init with one `walkOpfsTree` traversal; the 6.17 s phase no longer exists as such; re-measure is a map probe before slice A (not run this session — the spike spec, instrumentation and served asset were removed after the 09-01 record). Outcome (a) narrowed to first materialization (ADR-0415/0417: saved open never certifies install). no-COI toolchain Worker added as a consumer of the same `OpfsFsSync` (ADR-0372/0392/0393/0417; PR #332 in flight on `owner-storage.ts`, slice A after it). Namespace bump per selected `storage.namespace` root (ADR-0402). Legacy notice rides the ADR-0413 health snapshot. Candidate B / touch-fraction probe closed by ADR-0393/0406/0411. Re-bake route = `produceDependencySnapshot` (ADR-0386/0387). Verified unchanged: one-writer lock, `authority.snapshot()` copy (I4), deep-link `createScratch` premise (ADR-0414 keeps exclusions); sibling `fault-honest-opfs-persistence` — nothing landed. Merge conflicts: two append-only files; `packages/workbench/README.md` §Storage re-homed as §Persistence policy.

- 2026-09-12 — re-chart after targeted-page-reads (final-green PASS @ 93c05f463cc266eb47f36b4fd8cf528c9ad1d0b3): I4 proven in Chromium on T; file 0.0025 ms, directory 0.0303 ms, content reads 1/0. Full pr:check 25/25. Manifest T committed. Independent reviewer: /root/targeted_final_review; reference/perf verdict retained. Continue replica, I1/I2/I3/I5 remain.

- 2026-09-12 — re-cut: merge replica A+B into segmented-opfs-replica; omit temporary per-file delta substrate; preserve I1/I2/I3/I5 and Outcome (c). Independent DEC-2 /root/replica_decision selects existing OpfsFsSync + scheduler batch mode; ADR-0425 records supersessions and native guard. Native baseline T: drain 10.320 s / fresh-process offline preload 4.465 s; original I2 history retained. Public opening/npm/end-to-end acceptance remains required at goal close.

- 2026-09-13 — re-chart after segmented-opfs-replica (final-green PASS @ 631615fb94e4d027371ac972aada93b2deeec55d): one segmented base/append/compaction substrate serves Workbench and configured no-COI; native T/storage and production fault matrix accepted, 24/24 review rows. Native structural healing, read/GC lifetime and busy-Worker ownership gaps repaired with RED/mutants. pr:check25/25; packed scenarios and unchanged client budgets PASS. Separate clean-editor-handle finding retained in playground/editor-conflict-recovery, independently checked. Continue legacy/corrupt health notice and committed public T/npm/offline acceptance; I1/I2/I3/I5 goal composition remains.
