# Ledger — vitest-run-in-browser (append-only)

- 2026-09-15 — refined from the user question "can vitest run in rifty; which platform capabilities are missing"; probes + interview evidence: `docs/backlog/runtime-js/reference/vitest-run-in-browser-evidence.md`.
- 2026-09-15 — final written-result check pass 1 (RDY-6; fresh read-only reviewer subagent over the working tree on main 51440931a): 7 problems — child-8 contract vs fog, I2 over-broad (Worker only proven), I7 manifest precondition, stale-dir repeat-use, blocked_by vs map order, I3 `beforeExit`/`exit()` need, Node oracle from memory — all resolved in the landed revision.
- 2026-09-16 — final written-result check pass 2 (fresh read-only reviewer subagent over the working tree on main 51440931a): prior 7 resolved; 5 new — review-record forward claim, npm overrides semantics unverified, "same files" oracle wording, `vitest.config.ts`/`.ts`-tests scope gap (user asked), blocked_by prose — all but the scope answer resolved in the landed revision; Node v24.16.0 + npm 11.17.0 oracle evidence added.
- 2026-09-16 — user amendment before ready: `vitest.config.ts` + TypeScript tests in the claim ("Да: config.ts + .ts-тесты в заявке"); Outcome/scenario/I4/I7/map/acceptance amended; amended scenario re-run on main (evidence §I4-amended).
- 2026-09-16 — final written-result check pass 3 (fresh read-only reviewer subagent over the working tree on main 51440931a): pass-2 problems resolved; 4 wording/citation problems (Outcome "default config", I4 evidence sentence, vite TS-config proof scope, §Oracle framing) fixed before landing; scope re-check clean → `status: ready`.

- 2026-09-16 — landed refine + FIT set @ f872eb5c0 (reviewed revision for passes 1–3 = this set over main 51440931a).
- 2026-09-23 — run start (whole-goal hand-off, branch t3code/vitest-run-browser): re-chart before first pickup. Prior draft PRs #349/#351/#352 read as evidence only (their code is not authority; #349 took the rejected cac-rewrite route). New child 13 `message-port-ref-keepalive` (I4); item 5 widened to the claimed path's dynamic keys; item 11 re-cut to vitest's non-empty `execArgv`; item 8 absorbs kernel run-to-completion exit + Node `unref` shape; order 3→4→{7→8, 9}, 11 after 6/8/9/13.
- 2026-09-23 — re-chart after path-posix-win32-builtins (final-green PASS @ 14517ac745553d3299105c01b2454e5074fbbddd): `node:path/posix` + bare `path/posix` now the `path.posix` object (I6 path wall closed, `node:path/win32` narrowed out at pickup), item 2 dropped from the map and from item 12's `blocked_by`, and the verdict's REV-12 discoveries filed as drafts `runtime-js/path-win32-namespace`, `runtime-js/require-resolve-bare-builtin-id`, `runtime-js/builtin-esm-namespace-key-order`; Contract+RED @ 26fff2916c5de06b3b4cb8298db14bc45dc7f7a5
- 2026-09-23 — stop: STOP-1e raised by item 12 Contract+RED reception (I7 watch mode / other vite versions have no honest loud throw); asked in session; user answered "A: поправить I7 (Recommended)" → goal amend line; run continues.
- 2026-09-23 — re-chart after builtin-static-names-prototype-methods (final-green PASS @ e8ca0c17eb2fef1f02147a62aea48efa7b73a7b8): `process` now owns `cwd`/`chdir`/`hrtime`/`uptime`/`exit`/`kill`/`exitCode` so `import { cwd } from 'node:process'` links (I6 process wall closed), item 3 dropped from the map and from items 4/12 `blocked_by`, and the verdict's REV-12 discoveries filed as drafts `runtime-js/process-non-ipc-member-over-export`, `runtime-js/process-self-signal-listener`, `runtime-js/esm-module-code-sloppy-mode` (receiver probe widened at land: ESM bodies run sloppy); Contract+RED @ 5f355a65423d5bb3db6e1bfed48e9e18e9717b6f
- 2026-09-23 — re-chart after overrides-bare-version-spec (final-green PASS @ 6054e54bb879bc846d07f69deae463638abffeae): npm-spelled override values (`"vite": "8.0.16"`, ranges, `latest`) now pin the overridden edge (I1 closed, ADR-0451), item 1 dropped from the map and from item 12's `blocked_by`, and the verdict's REV-12 semver-matcher concern filed as draft `npm-client/semver-range-matching-node-semver-parity`; Contract+RED @ 43beed245648f0657143cc0c4705b54dc49627a1
- 2026-09-23 — re-chart after symbol-key-global-write-guard-precision (final-green PASS @ 2ff2623709872dbfffbdad553a3dd3abb5912625): ESM/CJS Function guards now check non-folding computed global-write keys at Node's coercion and throw only for `'Function'` (I6 guard walls closed, ADR-0444), item 5 dropped from the map and from item 12's `blocked_by`, and the verdict's REV-12 bypass-shape concern (pre-existing, identical at BASE) recorded as concrete shapes in draft `runtime-js/function-constructor-exhaustive-metaprogramming-ceiling`; Contract+RED @ fb441a51459a6555cc03791ad4c1e3198d55f58b
- 2026-09-23 — re-chart after readable-pipe-never-ends-process-stdio (final-green PASS @ 7bcc26f62fac86115066a28fe7272ce959cea5ea): `Readable.pipe(process.stdout|stderr)` now follows Node's `doEnd` rule (never ends the realm's own process stdio, unpipes at source end, `pipeline` ends its last stage; `node:process` read uncached, ADR-0458) so vitest's pool-child stdio pipe no longer crashes (I4 pipe wall closed), item 6 dropped from the map and from items 11/12 `blocked_by`, and the verdict's REV-12 Scope gaps filed as drafts `runtime-js/process-stdio-writable-end-surface`, `runtime-js/fs-readstream-pipe-node-options`, `runtime-js/pipe-unpipe-destination-events`; Contract+RED @ f47817a285428608fb28d9c9a6df1cfe6a1ae890
- 2026-09-23 — re-chart after message-port-ref-keepalive (final-green PASS @ 69cf64510ada931015955082e1cea6cc97630fd3): a manually referenced `MessagePort` is now a counted child-realm keepalive handle released by `unref()` or either pair end's `close()` (emnapi's napi async-work hold, I4 carrier; listener referencing and referenced-pair transfers stay named ❌ gaps, ADR-0447), item 13 dropped from the map, from items 11/12 `blocked_by` and its open question (answered by the Node control, evidence §Holder), and the verdict's REV-12 DataCloneError discovery filed as draft `runtime-js/worker-threads-kernel-postmessage-dataclone-error` (reentrant-getter hold, self-transfer error class and `MessageChannel` accessor stay NOTEs); Contract+RED @ e1676d095a25145fd4cf2f70f4cd6b84b2a2524c
- 2026-09-23 — re-chart after absent-builtin-members-loud-throws (final-green PASS @ 469a22f4b52a98088fee9387b0eb8558f93e598d): `fs.statfsSync`, `child_process.spawnSync` and `process.memoryUsage` (+ `.rss`) are now own named-loud members that link and throw `NotImplementedError` on call (I6 link/`bind` walls closed, ADR-0443 partially superseding ADR-0348 §2 — its owed DEC-2 decision review is still unrecorded, verdict concern), item 4 dropped from the map, its ordering prose and items 7/9/12 `blocked_by`, and the map's `--changed` line corrected to async `spawn` (unclaimed, unprobed); Contract+RED @ e727857522dd2faa97c839a25ea134aa830cf6ac
- 2026-09-23 — re-chart after vm-run-in-this-context-offsets (final-green PASS @ 6bb60606253edec3b206fce0aa94483e4c632a43): host-realm `vm.runInThisContext` / `Script#runInThisContext` now honour int32 `lineOffset`/`columnOffset` and own `sourceURL` naming in stacks and guest `prepareStackTrace` CallSites via one owned stack hook (I6 vm-offsets wall closed, ADR-0450; sandbox offsets stay named ❌ gaps), item 10 dropped from the map, its carrier open question (answered by ADR-0450) and item 12's `blocked_by`, and the verdict's concerns (int32-ceiling wrap, `ownSourceURL` guard without compat row, frozen-Error `sourceURL` gap, unpinned vitest own-`sourceURL` shape) stay REV-12 NOTEs; Contract+RED @ 75ea85bce3969246d1f3b31bc61f2d95863d937c
- 2026-09-23 — re-chart after child-process-advanced-ipc-serialization (final-green PASS @ 3fbc492d3056639bc249c895b98764a426956829): `fork(…, { serialization: 'advanced' })` now carries vitest's forks-pool frames (`undefined`, cycles, `Buffer`) as the sending realm's structured clone with Node's v8-serializer rules and named throws for platform objects (I4 advanced-IPC wall closed, ADR-0448 superseding goal.md's agent carrier line "structured-clone passthrough"; node-entry v5; inherited stdin read without holding the parent), item 9 dropped from the map, its ordering prose and items 11/12 `blocked_by`, and the verdict's REV-12 discoveries filed as drafts `runtime-js/ipc-send-after-disconnect-node-shape`, `runtime-js/ipc-disconnect-listener-keepalive`, `runtime-js/same-realm-child-top-level-const-process`; Contract+RED @ c378b464f8c20e5b75d57d4b9f2c22c4fe28051a
- 2026-09-24 — re-chart after process-lifecycle-events-exit-code (final-green PASS @ 9ec0b196004d6e4dd662a0c443c997ac745f3a50): `uncaughtExceptionMonitor`/`uncaughtException`/`unhandledRejection` listeners, one `'exit'` and Node `exit()` over an unset (`undefined`) `exitCode` now run on every claimed kernel child (I3 closed, ADR-0445 partially superseding ADR-0152 §1/§3 and extending ADR-0157 §1 — its owed DEC-2 decision review still unrecorded, verdict concern), with the STOP-1a fork (user 2026-09-24 "B: сузить + backlog (Recommended)") narrowing Parity 10 to rejections Chromium already delivered and routing already-queued Node callbacks to compat ⚠️ + draft `runtime-js/late-unhandled-rejection-drain` (kept), item 7 dropped from the map, its ordering prose, items 8/12 `blocked_by` and the answered ADR-shape open question, and the verdict's natural-exit-via-patched-`process.exit` concern linked as an open question for items 8/12 (other concerns stay REV-12 NOTEs); Contract+RED @ 6fdaaedf149044f3921eeb845a37541f87879aa3
